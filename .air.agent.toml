[build]
  cmd = "go build -o tmp/tclaw ."
  bin = "tmp/tclaw"
  full_bin = "tmp/tclaw serve"
  include_ext = ["go"]
  exclude_dir = ["cmd"]
  kill_delay = "500ms"
  rerun_delay = 500

[log]
  main_only = true
